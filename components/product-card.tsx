import Image from 'next/image';
import Link from 'next/link';
import { ArrowRight } from 'lucide-react';
import type { RiceProduct } from '@/data/products';
export function ProductCard({ product }: { product: RiceProduct }) { return <Link href={`/products/${product.slug}`} className="group overflow-hidden rounded-2xl border border-[#e7e3d9] bg-white shadow-[0_7px_24px_rgba(20,45,31,.06)] transition duration-300 hover:-translate-y-2 hover:border-[#d9bb67] hover:shadow-[0_20px_46px_rgba(20,45,31,.14)]"><div className="relative aspect-[1.2/1] overflow-hidden bg-[#f3f0e8]"><Image src={product.image} alt={product.name} fill sizes="(max-width:768px) 80vw, 280px" className="object-cover transition duration-500 group-hover:scale-105" /></div><div className="p-5"><span className="text-[10px] font-bold uppercase tracking-[.18em] text-[#9a7726]">{product.category}</span><h3 className="mt-2 font-serif text-[25px] font-semibold leading-none text-[#172019]">{product.name}</h3><p className="mt-3 line-clamp-2 text-[12px] leading-5 text-[#68726b]">{product.description}</p><span className="mt-4 inline-flex items-center gap-2 text-[11px] font-bold text-[#0B4A2B]">View Product <ArrowRight size={14} className="transition group-hover:translate-x-1" /></span></div></Link>; }
